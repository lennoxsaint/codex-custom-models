# OpenRouter key — used ONLY as a fallback when the macOS Keychain is unavailable
# (e.g. Windows/Linux). On macOS the key is stored in the Keychain, NOT here.
# Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-your-key-here
