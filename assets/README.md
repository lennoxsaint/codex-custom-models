# assets

Drop a neutral, original `fallback-icon.png` (1024x1024, square) here if you want the
built app to have an icon when the user's local Codex.app icon is not found.
Do NOT place any OpenAI/Codex artwork here — `*.icns` is git-ignored and the app
sources the real icon from the user's own install at runtime.
